import socket

def send_message(tcp_socket: socket.socket, message: str) -> bool:
    return True

def receive_message(tcp_socket: socket.socket, message: str) -> bool:
    return True

def send_file(tcp_socket: socket.socket, filename: str) -> bool:
    return True

def receive_file(tcp_socket: socket.socket, filename: str) -> bool:
    return Trues